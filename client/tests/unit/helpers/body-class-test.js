import { bodyClass } from '../../../helpers/body-class';
import { module, test } from 'qunit';

module('Unit | Helper | body class');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = bodyClass(42);
  assert.ok(result);
});
