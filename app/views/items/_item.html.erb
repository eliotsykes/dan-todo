<% @items.each do |item| %>
      <div id="item-<%= item.id %>">
        <%= item.name %> | <%= link_to "", [item.list, item], class: 'fa fa-trash-o', method: :delete, remote: true %>
      </div>
<% end %>