<% @items.each do |item| %>
  <div id="item-<%= item.id %>" class="item">
    <%= item.name %> | <%= item.days_left %> Days Left | <%= link_to "Delete", [item.list, item], class: 'fa fa-trash-o', method: :delete, remote: true %>
  </div>
<% end %>
