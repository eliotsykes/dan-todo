<% if @item.destroyed? %>
  $('#item-<%= @item.id %>').fadeOut( "slow" );
<% else %>
  $('#item-<%= @item.id %>').prepend("<div class='alert alert-danger'><%= flash.discard(:error) %></div>");
<% end %>