<%= simple_form_for @user.lists.new ,:url => {:action => :create, :controller => :lists} do |f| %>
  <%= f.input :title, label: 'Title', placeholder: "Enter list title" %>
  <%= f.submit 'Save' , class: 'btn btn-success' %>
<% end %>
